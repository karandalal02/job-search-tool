# Postgres connection string (from Neon). Leave unset to use local SQLite.
DATABASE_URL=

# Password required to use the web UI. Leave unset to disable the login gate (local dev only).
WEB_UI_PASSWORD=

# Random string used to sign session cookies. Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=
